This module extends `partner_email_role_assignment` to support role-based email
recipients for stock pickings.

It allows users to mark child contacts as delivery email recipients from the
partner form and updates the standard stock delivery email template so those
contacts are automatically included when sending pickings by email.
